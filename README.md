### For UWorld:

```
/Applications/Brave\ Browser.app/Contents/MacOS/Brave\ Browser --remote-debugging-port=9222 --user-data-dir=/tmp/brave-puppeteer-profile

npm i puppeteer

create folders `aud/`, `far/` next to capture_node_ss.mjs

node capture_node_ss.mjs
```

### For Becker:

Into dev tools console:
- copy paste import.js
- backer.js
